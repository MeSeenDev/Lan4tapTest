dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "Lan4tapTest"
include ':app'
include ':lan4-gate'
project(':lan4-gate').projectDir = file('app/common/lan4-gate/')
